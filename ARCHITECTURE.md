# 51Trip 架构说明

本仓库是一个基于静态 HTML 的旅行应用高保真原型，核心目标是快速呈现多场景界面与信息流，便于演示或作为后续开发的视觉基线。

## 信息架构
```
          ┌────────────┐
          │  首页.html  │
          └─────┬──────┘
                │
   ┌────────────┼────────────┐
   ▼            ▼            ▼
目的地.html  AI旅行规划.html  旅行助手.html
                                │
                                ▼
                           个人中心.html
```
- **首页**：充当顶层导航与灵感入口，可跳转至目的地、AI 规划或助手。
- **目的地**：纵览各地区、季节和主题，为用户提供灵感与故事。
- **AI 旅行规划**：收集偏好并展示 AI 生成的行程模板。
- **旅行助手**：面向出行执行阶段，整合行程、天气、工具与安全提示。
- **个人中心**：沉淀用户资产（行程、收藏、订单、会员权益），与助手互补。

## 页面结构要点
### 首页 (`首页.html`)
- 固定顶部搜索条 + 热门标签，模拟 App 顶栏。
- 模块化分区：热门城市、当季推荐、主题旅行、灵感墙与底部导航。
- 通过 `fixed` 底栏维持移动端 SPA 的导航体验。

### 目的地 (`目的地.html`)
- 标签式筛选 + 宽卡片布局突出重点目的地。
- “旅行者的故事”板块用来展示社区内容，卡片复用一致的阴影与圆角。
- 使用 `.no-scrollbar` 隐藏横向列表滚动条，保证视觉整洁。

### AI 旅行规划 (`AI旅行规划.html`)
- 偏好设置（滑杆、标签、复选框）→ 目的地输入 → 多日行程输出的线性流程。
- 行程内容按照「天」划分，使用色块和 icon 强调阶段。
- 底部 CTA（重新生成 / 保存方案）为未来接入服务预留位置。

### 旅行助手 (`旅行助手.html`)
- Hero 卡片展示当前行程与倒计时。
- “今日行程”采用时间轴视觉，醒目 icon + `border-l-2 border-dashed` 呈现顺序。
- “实用工具”、“交通出行”等模块为后续扩展 JS 功能留出网格/列表容器。

### 个人中心 (`个人中心.html`)
- 渐变背景 + 头像区域突出身份与会员等级。
- 列表式布局承载行程、收藏、订单和会员权益，统一使用卡片组件。
- 底部导航激活“我的”，与其他页面形成统一交互语义。

## 设计系统
- **样式体系**：Tailwind CSS（CDN）承担全部布局、间距和配色；适度结合自定义类（如 `.no-scrollbar`）。
- **色板**：以 `#FFB800` 为品牌主色，灰度文本与白色卡片营造轻量观感。
- **字体**：全局 `Noto Sans SC`，保证中文展示一致性。
- **图标**：Font Awesome 6.7.2（CDN）覆盖 UI icon，避免本地资源依赖。

## 数据与内容
- 目前所有文案、评分、图片均为示例数据，图片走腾讯云对象存储的公开链接。
- 如需动态化，可将静态内容抽离为 JSON（按模块划分）并用简单的模版渲染脚本注入。

## 扩展建议
1. **组件化**：迁移到 Vue/React/Svelte 等框架，复用导航、卡片、列表组件。
2. **状态管理**：利用 URL 参数或本地存储串联页面（例如选择的目的地同步给 AI 规划）。
3. **服务对接**：为 AI 规划、天气、订单等模块接入真实 API 时，可通过 Fetch + 渲染函数填充既有 DOM。
4. **样式主题化**：抽象 Tailwind preset 或 CSS 变量，支撑暗色 / 节日主题。

## 预览与部署
- 由于完全静态，可直接通过浏览器打开文件；若部署线上，放置于任意静态资源托管（GitHub Pages、Vercel、COS 等）即可。
- Tailwind 与 Font Awesome 依赖外网访问，生产部署时可考虑自托管以规避跨域或网络波动。
